# Design contract

Package này là ranh giới dữ liệu dùng chung giữa bảy demo.

Chỉ đưa vào đây:

- content;
- design token;
- asset mapping.

Không đưa React component hoặc bất kỳ styling implementation nào vào package.
Dữ liệu thật chỉ được thêm sau khi đã đối chiếu với Figma/reference; không tạo
placeholder đoán từ thiết kế.
