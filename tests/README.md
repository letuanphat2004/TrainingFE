# Tests

Các kiểm tra xuyên suốt workspace được chia thành:

- `smoke`;
- `visual`;
- `accessibility`.

Chỉ tạo test suite của trang đang triển khai. Screenshot sinh ra phải nằm trong
`artifacts`, không ghi đè ảnh chuẩn trong `reference`.
