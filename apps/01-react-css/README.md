# Demo 01 — React + CSS thuần

Demo này đi theo ba checkpoint:

1. HTML/CSS/JavaScript thuần;
2. React JavaScript;
3. React TypeScript.

Source hiện ở checkpoint React JavaScript ban đầu. Chỉ Home 1 được tạo; các
trang tiếp theo chỉ xuất hiện sau khi trang trước vượt qua gate.

Kiến trúc source:

```text
main → app → routes/pages → layouts/components
```

Thư mục chỉ được tạo khi có module thật. Không tạo placeholder cho tám trang.
